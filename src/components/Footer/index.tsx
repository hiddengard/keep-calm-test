import React, { FC } from 'react';
import styles from './Footer.module.scss';

const Footer: FC = () => {
  return (
    <footer className={styles.footer}>
      <h4>© TEST, 1022–2022</h4>
    </footer>
  );
};

export default Footer;
